{% extends './head.twig.tex' %}
{% import './title.twig.tex' as title %}
{% set settings = readJSON('./settings.json') %}
{% block packages %}
  {{parent()}}
  \usepackage[english]{babel}
  \usepackage[utf8]{inputenc}
{% endblock %}

{% block content %}
  {% if __.args.cov == "off" %}
  {% else %}
    \pagenumbering{roman}

    {{ title.generate(settings.title) }}
    {% block head %}{% endblock %}
    \clearpage
    \newpage
    \tableofcontents
    \listoftables
    \listoffigures
    \newpage
    \pagenumbering{arabic}
  {% endif %}
  {% block body %}No body Provided{% endblock %}

{% endblock %}
